{
  "name": "billdeskpro",
  "version": "1.0.0",
  "description": "Web-Based Billing System",
  "scripts": {
    "start": "cd server && npm install && node index.js",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}